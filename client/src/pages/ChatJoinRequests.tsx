import { JoinRequestsPage } from '../components/chat-invitations/index.js';

/** `/{role}/chat/:conversationId/join-requests` — owner-only review page. */
export const ChatJoinRequests = () => <JoinRequestsPage />;
